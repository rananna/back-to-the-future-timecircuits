blueprint:
  name: "BTTF Time Circuits: Display"
  description: >-
    **Display static text or an entity's state on the Time Circuits.**

    This is a powerful, all-in-one blueprint for showing information on the display.
    You can write a custom message, show the state of any Home Assistant entity, and apply various visual effects.
    It also supports sound, looping, and restoring the display to its previous state.
    This blueprint replaces the old 'Display Text' and 'Display Entity' blueprints.
  domain: script
  input:
    device:
      name: Time Circuits Device
      description: "Select the BTTF Time Circuits device you want to control."
      selector:
        device:
          integration: bttf_time_circuits

    target_row:
      name: Target Row
      description: "Choose which display row(s) the message should appear on."
      default: "TOP"
      selector:
        select:
          options:
            - { label: "Top Row (Destination Time)", value: "TOP" }
            - { label: "Middle Row (Present Time)", value: "MIDDLE" }
            - { label: "Bottom Row (Last Time Departed)", value: "BOTTOM" }
            - { label: "All Rows", value: "ALL" }

    data_source:
      name: "Data Source"
      description: "Choose what to display: a fixed message (Static Text) or the value of a sensor (Home Assistant Entity)."
      default: "static"
      selector:
        select:
          mode: dropdown
          options:
            - { label: "Static Text", value: "static" }
            - { label: "Home Assistant Entity", value: "entity" }

    # --- Static Text Inputs ---
    text_to_display:
      name: "[Static Text] Text to Display"
      description: "Enter the message to display. This field supports Home Assistant templates."
      default: ""
      selector:
        text: {}

    # --- Entity Inputs ---
    entity_to_display:
      name: "[Entity] Entity to Display"
      description: "Select the entity whose state or attribute you want to display."
      default: ""
      selector:
        entity: {}
    attribute_to_display:
      name: "[Entity] Attribute to Display"
      description: "Optional: If the entity has attributes, specify which one to display. Leave blank to use the entity's main state."
      default: ""
      selector:
        text: {}
    prefix:
      name: "[Entity] Prefix"
      description: "Optional: Text to add before the entity's value (e.g., 'Temp: ')."
      default: ""
      selector:
        text: {}
    postfix:
      name: "[Entity] Postfix"
      description: "Optional: Text to add after the entity's value (e.g., '°C')."
      default: ""
      selector:
        text: {}

    # --- Common Effect Inputs ---
    effect:
      name: "Visual Effect"
      description: "Choose the visual effect for displaying the text."
      default: "SET_TEXT"
      selector:
        select:
          options:
            - { label: "Set Text (Static)", value: "SET_TEXT" }
            - { label: "Marquee (Scrolling Text)", value: "MARQUEE" }
            - { label: "Pulse", value: "PULSE" }
            - { label: "Flash", value: "FLASH" }
            - { label: "Typewriter", value: "TYPEWRITER" }
            - { label: "Scramble Text", value: "SCRAMBLE_TEXT" }
            - { label: "Random Flicker", value: "RANDOM_FLICKER_TEXT" }

    duration:
      name: "Display Duration (s)"
      description: "Set the display duration for 'Set Text', 'Pulse', and 'Flash' effects. Other effects run until the text is displayed."
      default: 10
      selector:
        number: { min: 1, max: 300, unit_of_measurement: "s" }

    marquee_speed:
      name: "Marquee Speed (ms)"
      description: "Set the scroll speed for the marquee effect. Lower is faster."
      default: 100
      selector:
        number: { min: 20, max: 1000, unit_of_measurement: "ms" }

    flicker_speed:
      name: "Flicker Speed (ms)"
      description: "Set the speed for the 'Random Flicker' effect. Lower is faster."
      default: 50
      selector:
        number: { min: 10, max: 1000, unit_of_measurement: "ms" }

    # --- Audio Inputs ---
    audio_source:
      name: "Audio Source"
      description: "Optional: Choose a sound to play along with the visual effect."
      default: "none"
      selector:
        select:
          mode: dropdown
          options:
            - { label: "None", value: "none" }
            - { label: "Built-in Sound Effect", value: "builtin" }
            - { label: "Stream from Home Assistant", value: "media" }

    sound_effect:
      name: "Sound Effect"
      description: "Select a built-in sound effect to play from the device's SD card."
      default: "sys_beep.mp3"
      selector:
        select:
          options:
            - { label: "System Beep", value: "sys_beep.mp3" }
            - { label: "Arrival Chime", value: "arrival_chime.mp3" }
            - { label: "Electric Sparks", value: "electric_sparks.mp3" }
            - { label: "Engine Rev", value: "engine_rev.mp3" }
            - { label: "Flux Capacitor Power On", value: "flux_capacitor_power_on.mp3" }
            - { label: "Hum", value: "hum.mp3" }
            - { label: "Keypad Beeps", value: "keypad_beeps.mp3" }
            - { label: "Lock On", value: "lock_on.mp3" }
            - { label: "Relay Activation", value: "relay_activation.mp3" }
            - { label: "Time Travel", value: "time_travel.mp3" }

    media_file:
      name: "Media File"
      description: "Select an audio file from your Home Assistant media library to stream to the device."
      default: ""
      selector:
        media: {}

    volume:
      name: Volume
      description: "Set the volume for the sound effect or streamed media (0-100)."
      default: 80
      selector:
        number: { min: 0, max: 100, unit_of_measurement: "%" }

    # --- Looping and Restore ---
    repeat:
      name: "Repeat Count"
      description: "Number of times to repeat the alert. Built-in sounds will repeat; streamed audio plays once at the start."
      default: 1
      selector:
        number: { min: 1, max: 10, mode: box }

    restore_row:
      name: "Restore Row After"
      description: "If enabled, the display row will be restored to its previous state after the sequence completes."
      default: true
      selector:
        boolean: {}

mode: single

variables:
  device: !input device
  target_row: !input target_row
  data_source: !input data_source
  text_to_display: !input text_to_display
  entity_to_display: !input entity_to_display
  attribute_to_display: !input attribute_to_display
  prefix: !input prefix
  postfix: !input postfix
  effect: !input effect
  duration: !input duration
  marquee_speed: !input marquee_speed
  flicker_speed: !input flicker_speed
  audio_source: !input audio_source
  sound_effect: !input sound_effect
  media_file: !input media_file
  volume: !input volume
  repeat: !input repeat
  restore_row: !input restore_row

  # This logic determines the final text to be displayed based on the selected data source.
  value_to_display: >-
    {% if data_source == 'entity' and entity_to_display != "" %}
      {% if attribute_to_display != "" %}
        {{ state_attr(entity_to_display, attribute_to_display) }}
      {% else %}
        {{ states(entity_to_display) }}
      {% endif %}
    {% else %}
      {{ text_to_display }}
    {% endif %}
  final_text: "{{- prefix ~ (value_to_display | default('')) ~ postfix -}}"

sequence:
  - condition: template
    value_template: "{{ device != none }}"

  - service: mqtt.publish
    data:
      topic: "bttf_time_circuits/{{ (device_attr(device, 'identifiers') | list | last)[1] }}/sequencer/command"
      payload: >-
        {#-
          This macro constructs the JSON payload for the device's sequencer command.
          It builds a series of commands based on the selected text, effect, and sound,
          then wraps them in a track structure targeting the selected display row(s).
        -#}
        {%- macro generate_sequence_payload(target, effect_type, text, dur, m_speed, f_speed, restore, sound, sound_vol, repeat_count, audio_src) -%}
          {%- set ns = namespace(tracks=[]) -%}
          {%- set core_commands = [] -%}
          {#- Define which effects are self-blocking (run until complete) vs. timed (run for a set duration). -#}
          {%- set blocking_effects = ['MARQUEE', 'TYPEWRITER', 'SCRAMBLE_TEXT', 'RANDOM_FLICKER_TEXT'] -%}
          {%- set timed_effects = ['PULSE', 'FLASH'] -%}

          {# --- 1. Build the main visual command --- #}
          {%- set visual_command = {'command': effect_type, 'targetSegment': -1, 'stringParam': text} -%}
          {#- Add effect-specific parameters. -#}
          {%- if effect_type == 'MARQUEE' -%}
            {%- set visual_command = dict(visual_command, intParam=m_speed | int) -%}
          {%- elif effect_type == 'RANDOM_FLICKER_TEXT' -%}
             {%- set visual_command = dict(visual_command, intParam=f_speed | int) -%}
          {%- elif effect_type in timed_effects -%}
            {#- Pulse and Flash use intParam2 for their total duration. -#}
            {%- set visual_command = dict(visual_command, intParam2=(dur * 1000) | int) -%}
          {%- endif -%}
          {%- set core_commands = core_commands + [visual_command] -%}

          {# --- 2. For static effects like SET_TEXT, add a WAIT command to control how long it stays on screen. --- #}
          {%- if effect_type not in blocking_effects and effect_type not in timed_effects -%}
            {%- set wait_duration = (dur * 1000) | int -%}
            {%- set core_commands = core_commands + [{'command': 'WAIT', 'intParam': wait_duration}] -%}
          {%- endif -%}

          {# --- 3. Wrap the core commands in a loop if requested --- #}
          {%- if (repeat_count | int) > 1 -%}
            {%- set final_commands = [{'command': 'LOOP_START', 'intParam': repeat_count | int}] + core_commands + [{'command': 'LOOP_END'}] -%}
          {%- else -%}
            {%- set final_commands = core_commands -%}
          {%- endif -%}

          {# --- 4. Add the restore command if requested --- #}
          {%- if restore -%}
            {%- set final_commands = final_commands + [{'command': 'RESTORE_ROW'}] -%}
          {%- endif -%}

          {# --- 5. Generate the final track objects for each target row --- #}
          {%- set track_list = ['TOP', 'MIDDLE', 'BOTTOM'] if target == 'ALL' else [target] -%}
          {%- for row in track_list -%}
            {%- set current_commands = final_commands -%}
            {#- Add the sound command to the beginning of the sequence. -#}
            {#- We only add it to the first track to prevent it from playing multiple times for 'ALL' rows. -#}
            {%- if loop.first and audio_src == 'builtin' -%}
              {%- set device_volume = ((sound_vol / 100) * 21) | round(0) | int -%}
              {%- set sound_command = {'command': 'SOUND', 'stringParam': sound, 'intParam': device_volume} -%}
              {%- set current_commands = [sound_command] + current_commands -%}
            {%- endif -%}
            {%- set track_object = {'targetRow': row, 'commands': current_commands} -%}
            {%- set ns.tracks = ns.tracks + [track_object] -%}
          {%- endfor -%}

          {# --- 6. Return the raw JSON array of tracks --- #}
          {{- ns.tracks | to_json -}}
        {%- endmacro -%}
        {{- generate_sequence_payload(target_row, effect, final_text, duration, marquee_speed, flicker_speed, restore_row, sound_effect, volume, repeat, audio_source) -}}

  - choose:
      # This block handles streaming audio from Home Assistant.
      # It runs in parallel with the MQTT command above.
      - conditions:
          - "{{ audio_source == 'media' and media_file != '' }}"
        sequence:
          # Find the media_player entity associated with the selected device.
          - variables:
              media_player_entity: >
                {{ device_entities(device) | select('startswith', 'media_player.') | first | default('none') }}
          - condition: template
            value_template: "{{ media_player_entity != 'none' }}"
          # Set the volume on the device's media player.
          - service: media_player.volume_set
            target:
              entity_id: "{{ media_player_entity }}"
            data:
              volume_level: "{{ volume / 100 }}"
          # Play the selected media file.
          - service: media_player.play_media
            target:
              entity_id: "{{ media_player_entity }}"
            data:
              media_content_id: !input media_file
              media_content_type: "audio/mpeg"